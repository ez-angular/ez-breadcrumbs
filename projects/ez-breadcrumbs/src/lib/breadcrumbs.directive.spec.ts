import { EzBreadcrumbsDirective } from './breadcrumbs.directive';

describe('BreadcrumbsDirective', () => {
  it('should create an instance', () => {
    const directive = new EzBreadcrumbsDirective();
    expect(directive).toBeTruthy();
  });
});
